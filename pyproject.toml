[project]
name = "mininet-ai"
version = "0.1.0"
description = "A declarative experiment compiler and runtime for agentic networks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
	"agno[ollama,openai]>=3.0.11,<3.1",
	"httpx>=0.28.1",
	"jsonschema>=4.23.0",
	"pydantic>=2.13.3",
	"pyyaml>=6.0.3",
	"rich>=15.0.0",
	"typer>=0.25.0",
]

[project.scripts]
mininet-ai = "mininet_ai.cli:app"

[tool.uv]
package = true

[build-system]
requires = ["setuptools>=82.0.1"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "pyright>=1.1.414",
    "pytest>=9.1.1",
]

[tool.pyright]
include = ["mininet_ai", "examples/phase3", "tests"]
pythonVersion = "3.14"
typeCheckingMode = "basic"

[tool.setuptools.packages.find]
include = ["mininet_ai*"]
